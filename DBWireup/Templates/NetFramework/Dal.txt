using System;
using System.Collections.Generic;
using System.Data;
using System.Data.SqlClient;
using Roblox.Common;
using Roblox.Common.Persistence;
using Roblox.Mssql;
using Roblox.Entities.Mssql;

namespace ~NAMESPACE~
{
    /// <summary>
    /// Represents the Data Access Layer for the <see cref="~BIZCLASSNAME~"/> entity.
    /// </summary>
    /// <remarks>
    /// Generated on ~CURRENTDATE~ at ~CURRENTTIME~.
    /// </remarks>
    [Serializable]
    ~ACCESSIBILITY~ class ~DALCLASSNAME~
    {
        ~DALFIELDS~

        ~DALPROPERTIES~

        private static string ~CONNECTIONSTRING~
        {
            get { return ~CONNECTIONSTRINGVALUE~; }
        }

        ~ACCESSIBILITY~ ~DALCLASSNAME~()
        {
        }

        private static ~DALCLASSNAME~ BuildDAL(SqlDataReader reader)
        {
            var dal = new ~DALCLASSNAME~();

            while (reader.Read())
            {
		        ~READERPARAMETERS~
            }

            if (dal.ID == default(~IDTYPE~))
                return null;

            return dal;
        }
        
        ~PARAMFUNCTIONS~
    }
}