~ACCESSIBILITY~ static ~DALCLASSNAME~ ~PROCEDUREMETHOD~(~PARAMETERS~)
{
    ~PARAMETERVALIDATION~

    var queryParameters = new List<SqlParameter>
    {
        ~QUERYPARAMETERS~
    };

    using (var database = new GuardedDatabase(~CONNECTIONSTRING~))
    {
        return database.Get("[dbo].[~PROCEDURE~]", BuildDAL, queryParameters: queryParameters.ToArray());
    }
}