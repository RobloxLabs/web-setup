~ACCESSIBILITY~ static ~IDTYPE~ ~PROCEDUREMETHOD~(~PARAMETERS~)
{
    return EntityHelper.GetEntityCount<~COUNTTYPE~>(
        EntityCacheInfo,
        ~if(UNQUALIFIED)~
        CacheManager.UnqualifiedNonExpiringCachePolicy,
        ~else~
        new CacheManager.CachePolicy(
            CacheManager.CacheScopeFilter.Qualified,
            $"~CACHELOOKUP~"
        ),
        ~endif~
        $"~COLLECTIONID~",
        () => ~DALCLASSNAME~.~DALPROCEDUREMETHOD~(~DALPARAMS~)
    );
}