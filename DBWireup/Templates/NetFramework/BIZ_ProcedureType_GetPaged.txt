~ACCESSIBILITY~ static ICollection<~BIZCLASSNAME~> ~PROCEDUREMETHOD~(~PARAMETERS~)
{
    return EntityHelper.GetEntityCollection<~BIZCLASSNAME~, ~IDTYPE~>(
        EntityCacheInfo,
        ~if(UNQUALIFIED)~
        CacheManager.UnqualifiedNonExpiringCachePolicy,
        ~else~
        new CacheManager.CachePolicy(
            CacheManager.CacheScopeFilter.Qualified,
            $"~CACHELOOKUP~"
        ),
        ~endif~
        $"~COLLECTIONID~",
        () => ~DALCLASSNAME~.~DALPROCEDUREMETHOD~(startRowIndex + 1, maximumRows~if(!NOPARAMS)~, ~DALPARAMS~~endif~),
        Get
    );
}