~ACCESSIBILITY~ void ~PROCEDUREMETHOD~()
{
    ~PARAMETERVALIDATION~

    var queryParameters = new List<SqlParameter>
    {
		~QUERYPARAMETERS~
    };

    using (var database = new GuardedDatabase(~CONNECTIONSTRING~))
    {
        database.Update("[dbo].[~PROCEDURE~]", ID, queryParameters: queryParameters.ToArray());
    }
}