IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[dbo].[~GETBYPROPERTYPROCEDURE~]') AND type in (N'P', N'PC'))
BEGIN
	EXEC('CREATE PROCEDURE [dbo].[~GETBYPROPERTYPROCEDURE~] AS BEGIN SET NOCOUNT ON; END')
END
GO

/* FK Lookup */
/*  Don't use this.  It's garbage.  */
SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

ALTER PROCEDURE [dbo].[~GETBYPROPERTYPROCEDURE~]
(
	@~FKPROPERTYNAME~				~FKIDSQLTYPE~
)
AS

SET NOCOUNT ON

SELECT
	[ID]
FROM 
	[~TABLENAME~]
WHERE
	([~FKPROPERTYNAME~] = @~FKPROPERTYNAME~)

SET NOCOUNT OFF

RETURN
GO