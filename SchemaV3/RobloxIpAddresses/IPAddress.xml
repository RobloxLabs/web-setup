<?xml version="1.0"?>
<Entity Name="IPAddress">
  <!-- The properties of the entity -->
  <Properties>
	<Property Name="ID" Type="int" SqlType="INT" IsPrimaryKey="true" IsReadOnly="true" />
    <Property Name="Value" Type="int" SqlType="INT" />
    <Property Name="State" Type="bool" SqlType="BIT" />
    <Property Name="Expiration" Type="DateTime?" SqlType="DateTime" IsNullable="true" />
    <Property Name="Created" Type="DateTime" SqlType="DateTime" Default="getdate()" IsReadOnly="true" />
    <Property Name="Updated" Type="DateTime" SqlType="DateTime" Default="getdate()" IsReadOnly="true" />
  </Properties>
  
  <!-- The SQL procedures for the entity -->
  <Procedures>
	<!-- Deletes the entity with the given ID -->
	<Procedure Type="Delete">
		<Parameters>
			<Parameter Name="ID" />
		</Parameters>
	</Procedure>
	
	<!-- Gets the entity with the given ID -->
	<Procedure Type="Get">
		<Parameters>
			<Parameter Name="ID" />
		</Parameters>
	</Procedure>
	
	<!-- Gets the entities with the given IDs -->
	<!--Procedure Type="MultiGet">
		<Parameters>
			<Parameter Name="ID" />
		</Parameters>
	</Procedure-->
	
	<!-- Gets the entity with the given Value -->
	<Procedure Type="Get">
		<Parameters>
			<Parameter Name="Value" />
		</Parameters>
	</Procedure>
	
	<!-- Gets or Creates the entity with the given Value -->
	<Procedure Type="GetOrCreate">
		<Parameters>
			<Parameter Name="Value" />
		</Parameters>
	</Procedure>
	
	<!-- Inserts the entity into the table -->
	<Procedure Type="Insert" />
	
	<!-- Updates the entity with the given ID -->
	<Procedure Type="Update">
		<Parameters>
			<Parameter Name="ID" />
		</Parameters>
	</Procedure>
	
  </Procedures>
</Entity>